# models.py (Optional, for reference only)

class User:
    def __init__(self, id, name, email, password, created_at):
        self.id = id
        self.name = name
        self.email = email
        self.password = password
        self.created_at = created_at
